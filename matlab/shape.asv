function c = shape(sp, tp, thickness, mid)

if ~mid
    coeff = polyfit([sp(1),tp(1)],[sp(2),tp(2)],1);
    upper = coeff + [0, 0.5*thickness);
    lower = coeff - [0, 0.5*thickness);
    
    b = sp(2) + sp(1)/upper(1);
    l2 = 
    
else
    
end

end

